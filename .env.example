# ---- Server-only (Prisma / Postgres) ----
DATABASE_URL="postgresql://postgres:ENCODED_PASSWORD@db.PROJECT_ID.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"
DIRECT_URL="postgresql://postgres:ENCODED_PASSWORD@db.PROJECT_ID.supabase.co:5432/postgres?sslmode=require"

# ---- Client+Server (Supabase JS SDK) ----
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ---- Admin gating (used server-side; can be empty) ----
ALLOWED_ADMIN_EMAILS=
